[project]
name = "nautilus-trader-demo-strategies"
version = "0.1.0"
description = "Demo examples of various strategies and features implemented in Nautilus Trader."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "nautilus_trader==1.210.0"  # Latest stable version but, dev/alpha releases are suported - see nautilus_pypi
]

# Allow this only if you need to download alpha/dev versions of Nautilus from custom package index
# See latest content: https://packages.nautechsystems.io/simple/nautilus-trader/index.html
# [[tool.uv.index]]
# name = "nautilus_pypi"
# url = "https://packages.nautechsystems.io/simple"

[tool.ruff]
line-length = 30000  # we want to manage line length ourselves
fix = true
show-fixes = true